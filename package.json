{
  "name": "nexus-card",
  "private": true,
  "type": "module",
  "description": "Digital business card with follow-up reminders. Static frontend + Supabase backend.",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\"",
    "version-assets": "node scripts/version-assets.mjs",
    "check": "node scripts/version-assets.mjs --check && node --test \"tests/*.test.mjs\"",
    "serve": "python3 -m http.server 8742 --directory app"
  },
  "engines": {
    "node": ">=20"
  }
}
